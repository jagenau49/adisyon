<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.catsoftware.adisyon">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Adisyon">
        <activity android:name=".activitys.ResetDBActivity" />
        <activity android:name=".activitys.OrderDetailsOfDriverActivity" />
        <activity android:name=".activitys.DeletedOrdersActivity" />
        <activity android:name=".activitys.PaymentDriverActivity" />
        <activity android:name=".activitys.OrderAddActivity" />
        <activity android:name=".activitys.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>